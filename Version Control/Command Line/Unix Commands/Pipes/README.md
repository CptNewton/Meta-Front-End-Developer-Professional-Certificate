### As a developer working with the command line, you can use pipes to combine commands together.

- [x] True. <br>
      That's correct. When working with the command line, you can use pipes to combine commands together.
- [ ] False.
